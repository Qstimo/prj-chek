{
  "name": "cairn",
  "private": true,
  "packageManager": "pnpm@10.18.3",
  "engines": {
    "node": ">=22"
  },
  "pnpm": {
    "onlyBuiltDependencies": ["@swc/core", "esbuild"]
  },
  "scripts": {
    "test": "pnpm -r test",
    "typecheck": "pnpm --filter @cairn/shared build && pnpm -r typecheck",
    "build": "pnpm -r build",
    "e2e": "node scripts/e2e.mjs"
  },
  "devDependencies": {
    "otplib": "^12.0.1",
    "typescript": "^5.7.2"
  }
}

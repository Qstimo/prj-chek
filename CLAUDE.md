# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Состояние репозитория

Реализованы этапы 1–4: монорепо `apps/api` + `apps/web` + `packages/shared`, аутентификация с двухфакторной проверкой, проекты с секциями «Инфо», «Инфраструктура» (окружения, серверные параметры, домены), «Переменные» (шифрованные значения с версиями, раскрытие под журнал, импорт/выгрузка `.env`) и «Хроника» (лента событий и приёмный адрес: webhook сейчас, почта позже), выдачи доступа, журнал действий, развёртывание в контейнерах за реверс-прокси.

Источники правды:

- `project-registry-spec [eTsSSz].md` — техническое задание на систему **CAIRN**, источник правды по продукту;
- `docs/superpowers/specs/2026-08-2*-cairn-stage*-design.md` — согласованные дизайны этапов 1–4, источник правды по реализации;
- `docs/superpowers/plans/2026-08-2*-cairn-stage*.md` — планы реализации этапов по задачам.

Перед реализацией читай ТЗ и дизайн. ТЗ отвечает на вопрос «что и почему», дизайн — «как именно на этапе 1».

Команды разработки, проверок и развёртывания — в `README.md`. Коротко: `pnpm test` (Vitest во всех рабочих пространствах, интеграционные поднимают PostgreSQL через Testcontainers), `pnpm typecheck`, `pnpm build`. Линтера в проекте нет.

Структура файлов задана разделом «Структура файлов» плана этапа 1 — новые файлы размещай по нему.

## Зафиксированный стек

Монорепо на pnpm workspaces: `apps/web` — Next.js 15 (App Router, RSC, TanStack Query, shadcn/ui на Radix, Tailwind), `apps/api` — NestJS, `packages/shared` — контракт API на zod, единственный источник типов для обеих сторон. Хранилище — PostgreSQL через Drizzle. Развёртывание — Docker Compose. Тесты — Vitest в обоих приложениях, React Testing Library для компонентов, Testcontainers для интеграционных.

Язык интерфейса — русский, без библиотеки локализации.

Разработка ведётся по TDD: сначала падающий тест, затем реализация.

## Что такое CAIRN

Единый реестр независимых проектов: где проект развёрнут, жив ли он, чем настроен, что по нему решали, на какой стадии он находится. Единица учёта — **проект** (не окружение, как в менеджерах секретов, и не клиент, как в MSP-платформах).

У проекта шесть секций (закрытый список — секции являются единицами выдачи доступа):

1. **Инфо** — паспорт проекта, жизненный цикл (задаётся вручную, означает намерение).
2. **Инфраструктура** — окружения (прод/стейдж/дев), двухуровневая схема «проект → окружение» обязательна.
3. **Переменные** — самая чувствительная сущность: шифрование на уровне приложения, версионирование с откатом, значения раскрываются только отдельным действием с записью в журнал, импорт/выгрузка `.env`.
4. **Документация** — «как это устроено сейчас».
5. **Роадмап** — версии с чекпоинтами; прогресс вычисляется, не хранится. Единственная секция с публичной ссылкой.
6. **Хроника** — «как мы к этому пришли»; не смешивать с документацией.

## Архитектурные инварианты

Это жёсткие решения из ТЗ, а не пожелания:

- **Единая модель прав для людей и машин.** Три вида субъектов (пользователь, токен агента, приёмный адрес) живут в одной таблице выдач «субъект × проект × секция × уровень». Отдельной системы прав для машин быть не должно.
- **Четыре уровня доступа**: нет / метаданные / чтение / запись. «Нет» означает, что существование секции (и даже проекта) не раскрывается, и в базе выражается отсутствием строки выдачи, а не отдельным значением. Уровень «метаданные» существует прежде всего ради переменных: видеть список ключей, не видя значений.
- **Проверка прав — единой функцией на уровне выборки данных**, не в интерфейсе и не в компонентах. Это обязательное требование ТЗ (раздел 4.1). Репозитории обязаны принимать субъект в сигнатуре, чтобы проверку нельзя было забыть.
- **Отсутствие доступа — это `404`, а не `403`.** Ответ `403` сообщил бы, что объект существует. `403` уместен только когда доступ есть, но его уровень ниже требуемого для операции, и при обращении не-суперадмина к администрированию.
- **Журнал действий — неизменяемый, только на добавление**: кто, что, когда, над чем, человек или машина. Каждое раскрытие значения переменной — в журнал.
- **Агенты (MCP) — только чтение, никакой записи.** Профиль токена по умолчанию закрытый; доступ к значениям переменных — отдельная явная настройка, выключенная по умолчанию (риск утечки через контекст модели и внедрения инструкций через документацию — раздел 7.4).
- **Приёмный канал** — уникальный адрес проекта (почта + webhook); принадлежность проекту определяется адресом, без распознавания. Предложения модели (документация/чекпоинты из сводки) применяются только после подтверждения человеком.
- **Статус — автоматический** (health-check, TLS, срок домена), руками задаётся только «приостановлен».
- **Вне объёма** (раздел 1.4 — условие жизнеспособности продукта): задачи с исполнителями, трекинг времени, финансы, тикеты, комментарии, спринты. Чекпоинты роадмапа — без исполнителей, оценок, дат и вложенности.

## Нефункциональные ограничения

- Self-hosted, без внешних зависимостей для основной работы.
- Ключ шифрования хранится отдельно от базы; 2FA для суперадмина; секреты не попадают в логи и списочные ответы.
- Масштаб: десятки проектов, единицы пользователей — простота важнее производительности.

## Порядок реализации

Восемь этапов (раздел 10 ТЗ), после каждого — рабочий продукт. Ключевое: модель прав и понятие машинного субъекта проектируются на этапе 1 («Фундамент») — переделывать их на системе с живыми секретами крайне болезненно. Переменные — этап 4, MCP-доступ — последний, этап 8.

## Открытые вопросы

Из пяти вопросов раздела 12 ТЗ закрыты два: стек с развёртыванием и многопользовательская работа (нужна с этапа 1, включая интерфейс выдачи доступов). Остаются на будущие этапы: формат выгрузки конфигурации при деплое, нужен ли публичный статус-экран помимо роадмапа, глубина хранения версий переменных.

/** Подписи полей формы окружения. */
export const FIELD_LABELS = {
  name: 'Название',
  kind: 'Вид',
  serverId: 'Сервер',
  healthCheckPath: 'Путь проверки',
  notes: 'Заметки',
} as const;

/**
 * Подсказка к выбору машины.
 *
 * Второе предложение важнее первого: это единственное место, где система
 * честно говорит о границе своего знания. Разрешение адреса даёт точку
 * входа, а за CDN точкой входа служит чужая сеть.
 */
export const SERVER_HINT =
  'Не выбран — машина определится по адресу окружения. За CDN определится край сети, а не ваша машина';

/**
 * Подсказка к пути проверки: это ручка приложения, а не адрес.
 *
 * Сказано прямо, потому что раньше здесь спрашивали полный URL, и место
 * для адреса оказывалось вторым: проверка ходила на один хост, а сертификат
 * проверялся на другом.
 */
export const HEALTH_CHECK_PATH_HINT =
  'Ручка приложения, например /api/health. Адресами служат домены окружения';

/**
 * Подсказка к доменам: они и есть адрес окружения.
 *
 * Сказано прямо, потому что связь неочевидна из формы: вписанный домен
 * заводит корень в реестре сам, и по нему же идут проверки сертификата
 * и срока продления.
 */
export const DOMAINS_HINT =
  'Адрес окружения. Корневой домен попадёт в реестр сам, по домену идут проверки сертификата и срока продления';

/** Сколько ждать чужую страницу: она не обязана отвечать вовсе. */
export const REQUEST_TIMEOUT_MS = 3_000;

/** Сколько читать: заголовок живёт в первых килобайтах. */
export const MAX_BODY_BYTES = 256 * 1024;

/** Сколько переходов терпеть: трекеры любят один-два редиректа на вход. */
export const MAX_REDIRECTS = 3;
